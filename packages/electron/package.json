{
  "name": "better-ssh-client-electron",
  "private": true,
  "version": "1.1.2",
  "description": "BetterSshClient Desktop — SSH dashboard, terminal, and SFTP file manager.",
  "author": "BetterSshClient contributors",
  "homepage": "https://github.com/Sniphs98/BetterSshClient",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "electron-store": "^8.2.0",
    "glob": "^11.0.0",
    "smol-toml": "^1.3.1",
    "ssh2": "^1.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ssh2": "^1.15.1",
    "electron": "^33.2.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  }
}
